export const metadata = {
  title: 'Statistics - Instagram Engagement Assistant',
  description: 'View usage statistics and analytics',
};

export default function StatsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
